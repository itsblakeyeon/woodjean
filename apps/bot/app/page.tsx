export const dynamic = "force-dynamic";

export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: 32 }}>
      <h1>Woodjean Bot</h1>
      <p>Internal API surface for Telegram + cron + orders. See <code>app/api/*</code>.</p>
    </main>
  );
}
